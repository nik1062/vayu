name: PR CI - run tests

# No `paths-ignore` here on purpose. `CI gate` below is a required check, and a
# workflow that never starts never reports one - GitHub leaves the check
# "Expected" forever and the pull request can only be merged by an admin
# bypassing the rule. A docs-only change would be blocked outright.
#
# The filtering happens at the job level instead: `changes` always runs, `test`
# is skipped when nothing outside docs was touched, and `ci-gate` still reports.
# That is the case its "or skipped" branch was always written for.
on:
  pull_request:
    branches:
      - master

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  changes:
    name: Detect changes
    runs-on: ubuntu-latest
    # The filter reads the PR's file list from the API rather than cloning, so
    # this is all it needs. Stated explicitly rather than inherited, since a
    # fork PR's token is read-only regardless and this job must work for those.
    permissions:
      pull-requests: read
    outputs:
      code: ${{ steps.filter.outputs.code }}
    steps:
      - name: Check whether anything outside docs changed
        uses: dorny/paths-filter@v3
        id: filter
        with:
          # Same list the trigger used to carry. Leading `**` then exclusions:
          # `code` is false only when every changed file matches an exclusion.
          filters: |
            code:
              - '**'
              - '!docs/**'
              - '!**/*.md'
              - '!LICENSE'
              - '!.gitignore'
              - '!scripts/install-git-hooks.sh'

  test:
    name: Test on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    needs: changes
    if: needs.changes.outputs.code == 'true'
    permissions:
      contents: read
      checks: write
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        include:
          - os: ubuntu-latest
            preset: linux-prod
          - os: windows-latest
            preset: windows-prod
          - os: macos-latest
            preset: macos-prod
    timeout-minutes: 60
    env:
      VCPKG_COMMIT: 'cea592f4772491abdb7c483387a59ea89889f4be'
      VCPKG_BINARY_SOURCES: 'clear;files,${{ github.workspace }}/.vcpkg-archives,readwrite'
      SCCACHE_GHA_ENABLED: 'true'

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: Install system dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            ninja-build \
            build-essential \
            pkg-config \
            ca-certificates \
            shellcheck

      - name: Install system dependencies (macOS)
        if: runner.os == 'macOS'
        run: |
          command -v ninja || brew install ninja
          brew install shellcheck

      - name: Lint and test installer script
        if: runner.os != 'Windows'
        shell: bash
        run: |
          shellcheck install.sh
          bash scripts/test/install_test.sh

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: 'pnpm'
          cache-dependency-path: app/pnpm-lock.yaml

      - name: Cache vcpkg compiled packages
        uses: actions/cache@v5
        with:
          path: ${{ github.workspace }}/.vcpkg-archives
          key: vcpkg-${{ runner.os }}-${{ hashFiles('engine/vcpkg.json') }}-${{ env.VCPKG_COMMIT }}
          restore-keys: |
            vcpkg-${{ runner.os }}-${{ hashFiles('engine/vcpkg.json') }}-
            vcpkg-${{ runner.os }}-

      - name: Setup vcpkg
        uses: lukka/run-vcpkg@v11
        with:
          vcpkgDirectory: '${{ github.workspace }}/vcpkg'
          vcpkgGitCommitId: '${{ env.VCPKG_COMMIT }}'
          vcpkgJsonGlob: 'engine/vcpkg.json'

      # sccache is skipped on Windows: mozilla-actions/sccache-action@v0.0.10
      # (latest available, Apr 2022) fails its install step on the Windows Server
      # 2025 + Visual Studio 2026 image GitHub is rolling out across windows-latest
      # in June 2026. The install error surfaces as "Unable to locate executable
      # file: undefined" in the post-step and skips all downstream build/test
      # steps. Builds on Windows are slower without sccache but reliable.
      - name: Setup sccache
        if: runner.os != 'Windows'
        uses: mozilla-actions/sccache-action@v0.0.10

      - name: Configure sccache compiler launcher (non-Windows)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          echo "CMAKE_CXX_COMPILER_LAUNCHER=sccache" >> "$GITHUB_ENV"
          echo "CMAKE_C_COMPILER_LAUNCHER=sccache" >> "$GITHUB_ENV"

      - name: Build engine
        uses: lukka/run-cmake@v10
        with:
          cmakeListsTxtPath: '${{ github.workspace }}/engine/CMakeLists.txt'
          configurePreset: ${{ matrix.preset }}
          buildPreset: ${{ matrix.preset }}
          configurePresetAdditionalArgs: "['-DVAYU_BUILD_TESTS=ON']"

      - name: Run engine tests
        working-directory: engine
        shell: bash
        run: ctest --preset ${{ matrix.preset }} --output-on-failure --output-junit "$GITHUB_WORKSPACE/engine/test-results.xml"

      - name: Publish C++ test results
        uses: mikepenz/action-junit-report@v6
        if: always()
        with:
          report_paths: 'engine/test-results.xml'
          check_name: 'C++ Tests (${{ matrix.os }})'

      - name: Install frontend dependencies
        working-directory: app
        run: pnpm install --frozen-lockfile

      - name: Type-check frontend
        working-directory: app
        run: pnpm type-check

      - name: Run frontend tests
        working-directory: app
        run: pnpm test

  ci-gate:
    name: CI gate
    runs-on: ubuntu-latest
    # `changes` is a dependency as well as `test`, so a failure in the filter
    # cannot be read as "nothing to test". Without it, a broken `changes` job
    # skips `test`, and "skipped" would pass this gate on a change nobody ran.
    needs: [changes, test]
    if: always()
    steps:
      - name: Pass if tests succeeded or were skipped
        run: |
          changes="${{ needs.changes.result }}"
          result="${{ needs.test.result }}"
          if [[ "$changes" != "success" ]]; then
            echo "CI failed (change detection: $changes)"
            exit 1
          fi
          if [[ "$result" == "success" || "$result" == "skipped" ]]; then
            echo "CI passed (tests: $result)"
          else
            echo "CI failed (tests: $result)"
            exit 1
          fi
